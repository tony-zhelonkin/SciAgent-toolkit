#!/usr/bin/env bash
set -euo pipefail
root=$(git rev-parse --show-toplevel)
sciagent="$root/01_modules/SciAgent-toolkit/bin/sciagent"
if [[ -x "$sciagent" ]]; then
    "$sciagent" validate --project-dir "$root"
fi
