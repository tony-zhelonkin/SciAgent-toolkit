# tests/collision-allowlist.txt — intentional cross-namespace name overlaps.
#
# Each line: <name> <kind1>,<kind2>[,<kind3>] # rationale
# Kinds are one of: skill, agent, command.
# Kinds in the csv must be sorted in the canonical order skill,agent,command
# (the same order tests/test_no_duplicate_basenames.sh and the runtime helper
# emit). Lines starting with `#` and blank lines are ignored.
#
# New entries require PR review and a written rationale. Adding to this file
# is the only way to make tests/test_no_duplicate_basenames.sh tolerate a
# cross-namespace overlap; `sciagent lint --check toolkit` is the runtime
# soft-warn mirror.
#
# Mutation rule: this file shrinks when an overlap is removed and grows only
# with a deliberate family-overlap decision. Out-of-tree edits are a smell.
