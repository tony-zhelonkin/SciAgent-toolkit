# scripts/

Executable Python / Bash / R helpers the skill can invoke directly. Use for deterministic, repetitive, or parameter-heavy operations that are clearer as code than as prose. Keep each script self-documenting (docstring / usage comment at the top). Referenced from SKILL.md as `python scripts/<name>.py ...`.
