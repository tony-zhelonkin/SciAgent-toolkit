# assets/

Static templates, configs, example data, or figures bundled with the skill. Not executed — referenced in output (e.g. a plotting template) or loaded by scripts (e.g. a YAML config). Keep small; large example datasets belong elsewhere.
