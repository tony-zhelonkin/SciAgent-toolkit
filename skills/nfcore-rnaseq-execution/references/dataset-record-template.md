# Dataset Run Record — `<PROJECT_ID>`

> **Recipe:** `star-te-preprocessing` v`<X.Y.Z>`; config: `references/te_star.config` (snapshot below);
> runner: `nfcore-rnaseq-execution` v`<X.Y.Z>`.
>
> This is the reusable provenance template every dataset instantiates. It holds only the
> *filled, run-specific instance* (one run = one record); it is **not** a home for reusable
> knowledge — that lives in the canonical skills above, which this record points back to.
>
> **Durable home (ADR-E = Both):** keep the authoritative copy **beside the published results on
> `/data1`** (e.g. `/data1/.../<PROJECT_ID>/results_mm39_TE/run_record.md`) AND mirror it as an
> **Obsidian vignette** in the vault. `/data1` is the **source of truth**; the vignette is a
> searchable mirror. The working copy lives under `/scratch/nf-core/<PROJECT_ID>/` during the run.

---

## 1. Identity

| Field | Value |
|---|---|
| Project ID | `<PROJECT_ID>` (e.g. 14839-DM) |
| Species / genome build | `<e.g. Mus musculus, mm39 / GRCm39, GENCODE vM37>` |
| Sample count | `<N>` |
| Layout | `<paired-end; single-lane L005 / multi-lane>` |
| Run date | `<YYYY-MM-DD>` |
| Operator | `<name>` |

## 2. Pipeline version

- nf-core/rnaseq **pinned** `-r` version: `<e.g. 3.26.0>` (record the EXACT `-r` used — prior 13036/AdaW runs used 3.20.0; pin explicitly every run).
- Resolved revision hash: `<e.g. abc123def0 [3.26.0]>` (from the Nextflow banner / `.nextflow.log`)
- TE recipe re-validated against this `-r`? `<yes/no + note>` (changing the version can shift STAR/tool defaults).

## 3. Exact `nextflow run` command (VERBATIM)

> Paste the exact command as executed — no paraphrasing. This is the primary reproducibility artifact.

```bash
export NXF_UID=$(id -u); export NXF_GID=$(id -g)
nextflow run nf-core/rnaseq -r <PINNED_VERSION> -profile docker \
  -c /scratch/nf-core/<PROJECT_ID>/te_star.config \
  --project "<PROJECT_ID>" \
  --input <.../samplesheet.csv> \
  --outdir <.../results_mm39_TE> \
  --aligner star_salmon \
  --fasta <.../GRCm39.genome.fa.gz> \
  --gtf   <.../gencode.vM37.primary_assembly.annotation.gtf.gz> \
  --extra_star_align_args '<full random-one string — see star-te-preprocessing>' \
  --save_reference --save_align_intermeds --skip_qualimap \
  -work-dir /data2/nf-work/<PROJECT_ID>/work \
  -with-report <.../report.html> -with-timeline <.../timeline.html> -with-dag <.../flowchart.png> \
  -resume
```

## 4. `te_star.config` snapshot

- Source: `star-te-preprocessing/references/te_star.config` v`<X.Y.Z>` (canonical owner).
- Frozen snapshot in this run: `<path to the byte-identical copy, e.g. /scratch/nf-core/<PROJECT_ID>/te_star.config>`
- md5 of snapshot: `<md5sum>` (confirm it matches the canonical config)
- Any per-run config additions: `<e.g. -c skip_fqlint_dups.config — yes/no + reason>`

## 5. Samplesheet

- Path: `<.../samplesheet.csv>`
- Generated by: `nfcore-rnaseq-execution/scripts/make_samplesheet.sh <data_dir> <strandedness>`
- Rows: `<N>` (note any multi-lane samples that were merged by nf-core)

## 6. Strandedness (INFERRED-and-verified via MultiQC — do not assume)

- Samplesheet column: `<auto>` (pipeline subsamples 1M reads, infers strand via Salmon).
- **Inferred value (from MultiQC "Strandedness checks"):** `<unstranded | forward | reverse>` — Salmon vs RSeQC pass/fail: `<pass/fail>`.
- **Gene `-s` value used downstream:** `<0 | 1 | 2>` (must match the inferred call above).
- TE `-s` value: `<0 (unstranded, per the recipe)>`

## 7. Downstream TE counting

- SAF build command: `<grouped no-exon SAF, e.g. producing GRCm39_rmsk_TE_GROUPED_all_noExon.saf>`
- `runFeatureCounts_TE_and_genes.sh` invocation: `<exact command>`
- Toolkit version: `<TE-RNAseq-toolkit / bulkRNAseq_pipeline_scripts vX.Y.Z>`
- featureCounts flags: TE pass `-M -F SAF -s 0 -p --countReadPairs -B -C` (integer, no `--fraction`); gene pass `-s <verified>`.

## 8. Software versions

- `versions.yml`: `<path in the outdir>` (attach or link)
- Key tools: STAR `<v>`, Salmon `<v>`, samtools `<v>`, featureCounts/subread `<v>`, Nextflow `<v>`.

## 9. Output paths

- Outdir (published, `/data1`): `<.../results_mm39_TE>`
- Work dir (ephemeral, `/data2`): `<.../nf-work/<PROJECT_ID>/work>` (state: kept / cleaned)
- Deliverables: `report.html`, `timeline.html`, `flowchart.png`, `multiqc_report.html`.

## 10. QC results

| Metric | Value | Notes |
|---|---|---|
| Overall alignment rate | `<%>` | relax mismatch filter if <70% |
| TE proportion | `<%>` | expected ~3.8–6.0% |
| Matrix dims | `<TE meta-features> + <gene features>` | e.g. 1,243 TE + 78,317 gene |

## 11. Per-dataset deviations

> Anything that differed from the canonical recipe and WHY (e.g. `skip_fqlint_dups.config` used
> for duplicate read IDs; relaxed mismatch filter; different reference path). Keep this honest —
> it is how a future reviewer reconstructs why this run differs from the canon.

- `<deviation 1 + rationale>`
- `<deviation 2 + rationale>`
