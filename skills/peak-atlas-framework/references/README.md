# references/

Load-on-demand deep dives for the peak-atlas-* family. SKILL.md links here per
pipeline stage; children link UP into these rather than restating them.

| File | Stage / topic |
|---|---|
| `macs3-scatac-params.md` | CALL: scATAC MACS3 flags, 501bp normalization, chromosome/blacklist/N filters, Signac wrapper. |
| `iterative-overlap-merge.md` | MERGE: the algorithm in prose, relation to `iterative-peak-merging`, SPM/rule variant. |
| `support-voting.md` | TIER: `n_strategies` confidence, the compute-before-merge rule, the score boost. |
| `rare-celltype-protection.md` | FILTER: adaptive per-cluster threshold, stratified sampling, rescue, metadata pre-filter. |
| `validation-battery.md` | VALIDATE: FRiP / TSS / peaks-per-cell / marker / embedding / chromVAR. |
